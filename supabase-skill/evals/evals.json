{
  "skill_name": "supabase-security",
  "evals": [
    {
      "id": 1,
      "prompt": "Use the Supabase security skill to audit the department_stats view in the database. Is it safe to expose to all authenticated users?",
      "expected_output": "The skill should identify that the department_stats view is missing SECURITY INVOKER and bypasses RLS, exposing salary data to users who shouldn't have access.",
      "assertions": [
        "Identifies the department_stats view as a security risk",
        "Mentions security_invoker or SECURITY INVOKER",
        "Explains that the view bypasses RLS for the underlying tables",
        "Recommends adding WITH (security_invoker = true) or equivalent fix",
        "Does not execute DDL to modify the database — the view is audited only, not altered"
      ]
    }
  ]
}
